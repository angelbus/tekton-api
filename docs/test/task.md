| Task Name                                    | MVP | Description                                                                                                                            | Area           | Domain      |
| -------------------------------------------- | --- | -------------------------------------------------------------------------------------------------------------------------------------- | -------------- | ----------- |
| Define XDE Execution Payload Schema          | 1   | Define JSON payload structure sent from GRIDConnector to XDE (jobId, bookId, S3 paths, totalDeals, reconciliation flag, retries, ETA). | Solution       | XENA / GRID |
| Implement GRIDConnector Invocation API       | 1   | Create API integration that triggers GRID jobs with the prepared payload.                                                              | Solution       | XENA        |
| Implement FMTMService                        | 1   | Service responsible for receiving FMTM processing requests, preparing job payload, invoking GRIDConnector, and tracking job state.     | Solution       | XENA        |
| Implement CalibrationService                 | 1   | Service responsible for calibration workflows invoking GRID processing with a similar mechanism but different payload data.            | Solution       | XENA        |
| Implement XDE Worker Engine                  | 1   | Implement C++ engine responsible for reading payload, streaming deals, and executing compute tasks.                                    | Solution       | GRID        |
| Implement Deal Queue in XDE                  | 1   | Create dynamic deal queue allowing workers to pull deals to maximize CPU utilization.                                                  | Solution       | GRID        |
| Implement Worker Thread Pool                 | 1   | Create thread pool sized to CPU cores that processes deals in parallel.                                                                | Solution       | GRID        |
| Implement Result Aggregator                  | 1   | Implement aggregator component collecting worker results and writing final outputs.                                                    | Solution       | GRID        |
| Implement Output S3 Writer                   | 1   | Implement logic in XDE to write deal results into the output bucket.                                                                   | Solution       | GRID        |
| Implement Manifest File Generation           | 1   | Write `manifest.json` once job execution completes successfully.                                                                       | Solution       | GRID        |
| Configure S3 Output Buckets                  | 1   | Create S3 buckets for deal outputs, manifests, and progress files.                                                                     | Infrastructure | XENA        |
| Configure GRID Execution Environment         | 1   | Prepare container runtime environment and dependencies for XDE execution on GRID nodes.                                                | Infrastructure | GRID        |
| Implement S3 Event Notification              | 1   | Configure bucket event notifications to trigger only when `manifest.json` is written.                                                  | Infrastructure | XENA        |
| Implement XENALoader Lambda                  | 1   | Lambda triggered by manifest events that updates job status in XENA.                                                                   | Solution       | XENA        |
| Implement SQS Queue for Result Events        | 1   | Introduce SQS to buffer events between Lambda handlers and XENA services to avoid memory pressure on pods.                             | Infrastructure | XENA        |
| Implement Progress Tracking (_progress.json) | 2   | Periodically write `_progress.json` with processedDeals, lastProcessingDeal, ETA, etc.                                                 | Solution       | GRID        |
| Implement Reconciliation Job                 | 2   | Scheduled job that scans Redis for unfinished books and triggers reconciliation runs if timeout exceeded.                              | Solution       | XENA        |
| Implement Redis Book Tracking                | 2   | Store active book IDs and metadata in Redis while processing is ongoing.                                                               | Solution       | XENA        |
| Implement Book Completion Removal            | 2   | Remove book from Redis pending list when `manifest.json` is received.                                                                  | Solution       | XENA        |
| Implement Reconciliation Mode in XDE         | 2   | Support payload flag `reconciliation=true` so XDE skips already processed deals.                                                       | Solution       | GRID        |
| Implement Deal Result Existence Check        | 2   | XDE checks if deal output exists in S3 before recomputation in reconciliation mode.                                                    | Solution       | GRID        |
| Implement Retry and Exponential Backoff      | 2   | Add retry logic with exponential backoff for deal processing failures.                                                                 | Solution       | GRID        |
| Implement Job Retry Counter                  | 2   | Track `bookRetries` in payload and progress metadata.                                                                                  | Solution       | XENA / GRID |
| Implement CPU-Aware Thread Pool              | 2   | Configure worker threads based on hardware concurrency of GRID slot.                                                                   | Solution       | GRID        |
| Implement Optional CPU Affinity              | 2   | Optionally pin worker threads to specific cores to improve cache locality.                                                             | Solution       | GRID        |
| Implement Payload Optimization               | 2   | Pass metadata such as totalDeals and bookSize in payload to avoid redundant parquet metadata scans.                                    | Solution       | XENA / GRID |
| Implement S3 Manifest Event Filtering        | 2   | Configure S3 events using suffix filtering (`manifest.json`) to avoid unnecessary Lambda invocations.                                  | Infrastructure | XENA        |
| Implement Monitoring and Metrics             | 2   | Add metrics for job runtime, CPU utilization, and throughput.                                                                          | Infrastructure | XENA / GRID |
| Implement Observability Dashboards           | 2   | Build dashboards tracking GRID throughput, book completion times, and reconciliation runs.                                             | Infrastructure | XENA        |
